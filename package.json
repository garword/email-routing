{
  "name": "email-routing-manager",
  "version": "1.0.0",
  "description": "Email Routing Manager with bilingual support (EN/ID)",
  "main": "next start",
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build && cp -r .next/static .next/standalone/.next/ && cp -r public .next/standalone/",
    "start": "NODE_ENV=production node .next/standalone/server.js",
    "deploy": "npm run build && git add . && git commit -m \"Add Email Routing Manager v1.0.0\" && git push origin main"
  },
  "keywords": ["email-routing", "cloudflare", "bilingual", "dashboard"],
    "author": "Email Routing Manager Team",
    "license": "MIT"
  }
}